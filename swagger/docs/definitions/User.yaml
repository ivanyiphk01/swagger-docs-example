definitions:
  User:
    type: object
    required:
      - firstName
      - lastName
      - age
      - id
    properties:
      id:
        type: string
      firstName:
        type: string
        description: first name
      lastName:
        type: string
        description: last name
      age: 
        type: string
        description: age of the user