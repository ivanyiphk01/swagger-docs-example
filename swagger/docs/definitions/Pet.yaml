definitions:
  Pet:
    type: object
    required:
      - id
      - name
      - species
    properties:
      id:
        type: string
      name:
        type: string
        description: name of the pet
      species:
        type: string
        description: spieces of the pet
      isSick:
        type: boolean
        description: is the pet sick